# Brewfile

tap "homebrew/bundle"        # Adds brew bundle support (Brewfile workflow)
tap "homebrew/cask"          # GUI apps and large binary packages
tap "homebrew/cask-fonts"    # Fonts as casks (Nerd Fonts, etc.)
tap "gromgit/brewtils"                 # Third-party tap that provides taproom

# CLI tools (formulae)
brew "bat"                             # Better cat with syntax highlighting
brew "btop"                            # Modern resource monitor (CPU/RAM/disk/network) in the terminal
brew "csvlens"                         # Terminal CSV viewer (browse/search/filter columns interactively)
brew "ddgr"                            # DuckDuckGo search from the terminal (CLI)
brew "dust"                            # Better du: clearer disk-usage output (Rust)
brew "fastfetch"                       # ASCII logo + system info on shell startup (via ~/.zshrc fastfetch block)
brew "ffmpeg"                          # Audio/video conversion and processing Swiss army knife
brew "findutils"                       # GNU find, xargs, locate, updatedb (g-prefixed tools)
brew "fx"                              # Interactive JSON viewer for the terminal
brew "gemini-cli"                      # Google Gemini CLI for prompting/chatting from the terminal
brew "gh"                              # Official GitHub CLI
brew "glow"                            # Render Markdown in the terminal (pretty pager)
brew "gromgit/brewtils/taproom"        # Interactive TUI for browsing/managing Homebrew formulae + casks
brew "iftop"                           # Real-time network bandwidth monitor per connection
brew "imagemagick"                     # Image conversion and manipulation toolkit
brew "jq"                              # JSON processor (slice, filter, transform)
brew "jless"                           # Pager for exploring JSON (interactive, like less + jq)
brew "lazydocker"                      # Terminal UI for Docker + docker compose (containers, logs, stats)
brew "lazygit"                         # Terminal UI for git (fast staging, logs, branches, conflicts)
brew "ledger"                          # Accounting CLI for plain-text finance tracking
brew "libjpeg"                         # JPEG codec library (dependency for image tools)
brew "libmemcached"                    # libmemcached client library (Memcached support)
brew "lsd"                             # Prettier ls replacement (your ~/.zshrc aliases ls to lsd when installed)
brew "magic-wormhole"                  # Simple, secure file transfer between computers
brew "markdown"                        # CLI Markdown processor/converter
brew "mas"                             # Mac App Store CLI (install apps by ID)
brew "mc"                              # Midnight Commander terminal file manager
brew "mole"                            # Deep clean and optimize macOS (cleanup utility)
brew "ncdu"                            # TUI disk-usage viewer (fast way to find big folders)
brew "neovim"                          # Modern Vim fork with LSP-friendly ecosystem (aka nvim)
brew "node"                            # Node.js runtime (JS tooling, scripts)
brew "pure"                            # Pretty, minimal and fast ZSH prompt
brew "ranger"                          # Keyboard-driven file browser in the terminal
brew "rtk"                             # Filters and compresses command outputs before they reach the LLM context
brew "speedtest-cli"                   # Internet speed test from the terminal
brew "stow"                            # Symlink manager for dotfiles (keeps configs organized via a “symlink farm”)
brew "tree"                            # Directory tree printer
brew "vim"                             # Vim editor
brew "watch"                           # Re-run a command every N seconds and show live output
brew "wget"                            # File downloader (HTTP/HTTPS/FTP)
brew "yt-dlp"                          # YouTube and media downloader (modern replacement for youtube-dl)
brew "zsh-syntax-highlighting"         # Syntax highlighting as you type in zsh

# GUI apps (casks)

cask "balenaetcher"          # Flash images to SD cards and USB drives
cask "claude-code"                     # Claude Code terminal coding assistant (Anthropic)
cask "cleanshot"             # CleanShot X screenshot + screen recording tool
cask "copilot-cli"                     # GitHub Copilot CLI agent for the terminal
cask "docker"                # Docker Desktop for macOS
cask "github"                # GitHub Desktop app
cask "google-chrome"         # Chrome browser
cask "iterm2"                # Better terminal app than Terminal.app
cask "popclip"               # Pop-up action bar when you select text
cask "private-internet-access"         # PIA VPN client (Private Internet Access)
cask "tunnelbear"            # TunnelBear VPN client for macOS
cask "visual-studio-code"    # VS Code editor

curl -Ls https://raw.githubusercontent.com/sindresorhus/iterm2-snazzy/main/Snazzy.itermcolors > /tmp/Snazzy.itermcolors && open /tmp/Snazzy.itermcolors
